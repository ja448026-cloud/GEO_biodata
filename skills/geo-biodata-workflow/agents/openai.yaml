interface:
  display_name: "GEO_biodata Workflow"
  short_description: "Legacy entry for GEO_biodata shared scripts"
  default_prompt: "Use $geo-biodata-workflow only as a compatibility entry point, then route to the narrow GEO_biodata skill that matches the task."
